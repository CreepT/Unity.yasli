using System;
using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class Network : MonoBehaviour {
/*
        public static IEnumerator POST(WWWForm data) {
          var Query = new WWW("https://cyberchronicles-api.herokuapp.com/api", data);
          yield return Query;
          Query.Dispose();
        };

        public static IEnumerator GET(string query) {
          WWW Query = new WWW("https://cyberchronicles-api.herokuapp.com/api" + query);
          yield return Query;
          Query.Dispose();
        };

        public void getLanguage() {
          StartCoroutine(GET("/language"));
        };
*/
}
